# MCP tools

Phase 2 will expose typed wrappers over the same Core pipeline:

- `inspect_template`
- `validate_model`
- `render_document`
- `validate_docx`

No rendering or validation logic may be implemented in this project.
