# Output

All process output belongs here:

- stdout: JSON with `--json`, otherwise only the resulting path or compact
  command result;
- stderr: human-oriented logs;
- no banners, progress animations, or prompts;
- failures always include an exit code, stable diagnostic code, JSON path when
  applicable, and actionable hint.
